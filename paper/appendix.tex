\documentclass[letterpaper]{article}
\usepackage[submission]{aaai2027}
\usepackage{amsmath,amssymb,booktabs,graphicx,algorithm,algpseudocode}

\title{Technical Supplement:\\When Short Trajectories Lie -- IIGC Framework Details and Extended Results}
\author{Anonymous Submission}

\begin{document}
\maketitle

\section{Environment Details}

\subsection{510K Card Game}

510K is a four-player trick-taking game with a standard 52-card deck.
Cards 5, 10, K carry point values (5, 10, 10).
The lead player sets the pattern (single, pair, straight, bomb, 510K pattern, etc.); others must follow with a higher-rank play or pass.
The trick winner collects all 5/10/K points from the trick.

\medskip
\noindent\textbf{Action space:} 300 discrete actions (0 = pass, 1--299 = pattern index).
\noindent\textbf{Observation space:} 112-dim Box (116-dim for OBVIOUS mode, with 4-bit teammate indicator).
\noindent\textbf{15 pattern types:} SINGLE, PAIR, THREE, THREE\_ONE, THREE\_PAIR, STRAIGHT (5+), CONSECUTIVE\_PAIRS (3+), AIRPLANE\_NONE, AIRPLANE\_SINGLE, AIRPLANE\_PAIR, BOMB, NONSUIT\_510K, SUIT\_510K, RED\_A\_SINGLE, RED\_A\_PAIR.

\medskip
\noindent Four modes share identical card mechanics but differ in team structure:

\begin{itemize}
\item \textsc{Single}: solo-competitive. Game ends when first player empties hand.
  Winner $+30$, losers $-10$ each (plus own 510K points).
\item \textsc{Static}: fixed teams (players 0\&2 vs.\ 1\&3). Both teammates must finish.
  Winners $\times 2$ multiplier, losers $\times 1$.
\item \textsc{Dynamic}: teams determined by hidden red-Ace distribution (holders of
  A$\heartsuit$/A$\diamondsuit$ form one team). Same scoring as \textsc{Static}.
  Red A is the strongest single; red-A pair is the strongest bomb.
\item \textsc{Obvious}: identical card mechanics to \textsc{Dynamic}, but team membership
  is observable as a 4-bit observation. This isolates \textit{information structure}
  from card mechanics.
\end{itemize}

\medskip
\noindent\textbf{Behavioral features for path integral.}
Seven mode-independent features ($\phi_1$--$\phi_7$), all with VIF $< 2$:
\begin{itemize}
\item $\phi_1$ \textbf{MyScore}: cumulative 510K points, normalized by 150.
\item $\phi_2$ \textbf{MyHandSize}: fraction of hand played, $(13 - |h|)/13$.
\item $\phi_3$ \textbf{MyStrength}: average card rank, mapped to $[0,1]$.
\item $\phi_4$ \textbf{TrickScore}: points at stake in current trick, normalized by 100.
\item $\phi_5$ \textbf{PassCount}: consecutive passes in current trick, normalized.
\item $\phi_6$ \textbf{ScoreSpread}: std.\ of scores across players, normalized by 50.
\item $\phi_7$ \textbf{SuppressionGap}: rank gap to leader's strongest card, normalized by 12.
\end{itemize}
Feature ablation: deleting any single feature preserves the path-length ordering
$\textsc{Single} > \textsc{Static} > \textsc{Dynamic}$ (7/7 subsets).

\medskip
\noindent\textbf{OBVIOUS construction.}
The \textsc{Obvious} observation is 116-dim (112 base + 4 teammate-indicator bits).
Bit $i=1$ if player $i$ is on the agent's team. This is the only structural
difference from \textsc{Dynamic} (112-dim).

\subsection{Toy Matching Environment}

A minimal 2-action contextual bandit that isolates IIGC:

\begin{itemize}
\item \textbf{State}: 1-dim (constant 0) in HIDDEN; 2-dim (one-hot partner type) in REVEALED.
\item \textbf{Action}: $a \in \{0, 1\}$. Agent chooses which partner type to match.
\item \textbf{Partner}: hidden binary type $p \in \{0, 1\}$, fixed per episode.
\item \textbf{Reward}: $+1$ if $a = p$, $-1$ if $a \neq p$.
\item \textbf{Training}: PPO, 10K steps, 10 seeds per condition.
\end{itemize}

In HIDDEN mode, the agent receives no information about $p$.
Gradients from episodes where $p=0$ and $p=1$ cancel completely ($\kappa \approx 0$).
In REVEALED mode, the agent observes $p$ directly and learns the optimal
type-matching policy.

\subsection{Overcooked Environment}

Grid-world cooperative cooking game with configurable partner roles:

\begin{itemize}
\item \textbf{State}: 96-dim observation (grid layout, object positions, inventory).
\item \textbf{Action}: 6 discrete actions (up/down/left/right/interact/stay).
\item \textbf{Partner roles}: CHEF (cooks soup) or WAITER (delivers soup).
\item \textbf{Static mode}: partner role visible as 1-hot observation.
\item \textbf{Dynamic mode}: partner role hidden, switches every 30 steps.
\item \textbf{Reward}: sparse $+20$ per soup delivered.
\item \textbf{Training}: PPO, 1M steps, 8 parallel envs, 8 seeds per mode.
\end{itemize}

\section{$\kappa$ Computation}

\subsection{Pseudocode}

\begin{algorithm}[H]
\caption{Computing the Relational Gradient Retention Ratio $\kappa$}
\begin{algorithmic}[1]
\State \textbf{Input:} Trained policy $\pi_\theta$, environment with two hidden
  configurations $A$ and $B$, number of rollouts $N$, discount $\gamma$
\State \textbf{Output:} $\kappa \in [0, 1]$
\State
\State $g_A \gets \mathbf{0}$ \Comment{Accumulated gradient for config $A$}
\State $g_B \gets \mathbf{0}$ \Comment{Accumulated gradient for config $B$}
\For{$i \gets 1$ to $N$}
  \State $\tau_A \gets \text{Rollout}(\pi_\theta, \text{config}=A)$
  \State $\tau_B \gets \text{Rollout}(\pi_\theta, \text{config}=B)$
  \State $g_A \gets g_A + \frac{1}{N} \sum_{t} \nabla_\theta \log \pi_\theta(a_t|o_t) \cdot R(\tau_A)$
  \State $g_B \gets g_B + \frac{1}{N} \sum_{t} \nabla_\theta \log \pi_\theta(a_t|o_t) \cdot R(\tau_B)$
\EndFor
\State
\State $g_{\text{mixed}} \gets \frac{1}{2}(g_A + g_B)$
\State $\kappa \gets \|g_{\text{mixed}}\|^2 \;/\; \tfrac{1}{2}(\|g_A\|^2 + \|g_B\|^2)$
\State \textbf{return} $\kappa$
\end{algorithmic}
\end{algorithm}

\subsection{Implementation Notes}

\begin{itemize}
\item Gradients are computed via REINFORCE $\nabla_\theta \log \pi(a|o) \cdot R(\tau)$,
  not via backpropagation through a training loss.
\item $\kappa$ requires only \textbf{rollout access} to the policy, not training access.
  It can be computed post-hoc from any saved checkpoint.
\item $N \approx 30$ rollouts per configuration suffices for stable $\kappa$ estimates.
\item For value-based methods (DQN), we substitute the TD-loss gradient
  $\nabla_\theta (Q_\theta(s,a) - y)^2$ in place of the REINFORCE gradient.
\item When $\|g_A\|^2 + \|g_B\|^2 = 0$ (zero gradient energy), define $\kappa = 0.5$.
\end{itemize}

\section{Proofs of Propositions}

\subsection{Proposition 1 (Pythagorean Decomposition)}

Let $g_A, g_B \in \mathbb{R}^d$ be the policy gradients under configurations $A$ and $B$.
Define $g_+ = \frac{g_A + g_B}{2}$ (shared component) and $g_- = \frac{g_A - g_B}{2}$
(differential component).

\begin{align}
\|g_A\|^2 + \|g_B\|^2
&= \|g_+ + g_-\|^2 + \|g_+ - g_-\|^2 \\
&= \bigl(\|g_+\|^2 + \|g_-\|^2 + 2 g_+ \!\cdot\! g_-\bigr) \\
&\qquad + \bigl(\|g_+\|^2 + \|g_-\|^2 - 2 g_+ \!\cdot\! g_-\bigr) \\
&= 2\bigl(\|g_+\|^2 + \|g_-\|^2\bigr)
\end{align}

Therefore $\frac{\|g_A\|^2 + \|g_B\|^2}{2} = \|g_+\|^2 + \|g_-\|^2$.
Since $\kappa = \|g_+\|^2 / (\|g_+\|^2 + \|g_-\|^2)$, it follows that $\kappa$ is the
fraction of total gradient energy retained after aggregation.

\subsection{Proposition 2 (Effective Learning Signal)}

Under balanced configuration mixing ($p_A = p_B = 1/2$), the expected per-step
gradient during training is:
\begin{equation}
\mathbb{E}[g_{\text{train}}] = \frac{1}{2}(g_A + g_B) = g_+
\end{equation}
The effective gradient magnitude satisfies:
\begin{equation}
\|g_+\|^2 = \kappa \cdot \frac{\|g_A\|^2 + \|g_B\|^2}{2}
\end{equation}
When $\kappa \to 0$, $\|g_+\|^2 \to 0$ and the policy receives no effective
learning signal regardless of the per-configuration gradient magnitudes.

\subsection{Proposition 3 (Gradient Structure Distinction)}

For policy-gradient methods, $g_r^{\text{PG}}$ reflects the direction of
improvement in action preferences for configuration $r$.
When optimal actions differ across configurations, $g_A^{\text{PG}} \approx -g_B^{\text{PG}}$
and $\kappa_{\text{PG}} \to 0$.

For TD-learning methods, $g_r^{\text{TD}}$ minimizes prediction error under a
\textit{shared} Q-function. Both configurations push toward representations
that minimize error across the mixture, producing aligned updates:
$\kappa_{\text{TD}} > \kappa_{\text{PG}}$ under identical information asymmetry.

\section{Full Path Integral Data}

\begin{table}[h]
\centering
\small
\begin{tabular}{lcccc}
\toprule
Policy (seed) & Path Length & Endpoint Dist. & Curvature \\
\midrule
SINGLE (41)   & 0.340 & 0.046 & 7.3$\times$ \\
SINGLE (51)   & 0.482 & 0.068 & 7.1$\times$ \\
SINGLE (52)   & 0.561 & 0.034 & 16.6$\times$ \\
SINGLE (53)   & 0.454 & 0.108 & 4.2$\times$ \\
SINGLE (54)   & 0.443 & 0.051 & 8.8$\times$ \\
STATIC (41)   & 0.253 & 0.071 & 3.6$\times$ \\
STATIC (61)   & 0.342 & 0.086 & 4.0$\times$ \\
STATIC (62)   & 0.490 & 0.073 & 6.7$\times$ \\
STATIC (63)   & 0.281 & 0.048 & 5.9$\times$ \\
STATIC (64)   & 0.278 & 0.021 & 13.5$\times$ \\
DYNAMIC (41)  & 0.299 & 0.046 & 6.5$\times$ \\
DYNAMIC (42)  & 0.231 & 0.112 & 2.1$\times$ \\
DYNAMIC (43)  & 0.367 & 0.026 & 14.3$\times$ \\
DYNAMIC (44)  & 0.274 & 0.031 & 8.9$\times$ \\
OBVIOUS (71)  & 0.203 & 0.100 & 2.0$\times$ \\
OBVIOUS (72)  & 0.355 & 0.122 & 2.9$\times$ \\
OBVIOUS (73)  & 0.366 & 0.051 & 7.2$\times$ \\
OBVIOUS (74)  & 0.302 & 0.022 & 13.6$\times$ \\
OBVIOUS (75)  & 0.318 & 0.036 & 8.9$\times$ \\
OBVIOUS (76)  & 0.391 & 0.085 & 4.6$\times$ \\
OBVIOUS (77)  & 0.283 & 0.043 & 6.6$\times$ \\
OBVIOUS (78)  & 0.404 & 0.035 & 11.5$\times$ \\
\bottomrule
\end{tabular}
\caption{Full path integral data for all 22 seeds across 4 modes.}
\end{table}

\section{Extended $\kappa$ Data}

\begin{table}[h]
\centering
\small
\begin{tabular}{lllll}
\toprule
Env & Algo & Mode & $\kappa$ (mean $\pm$ std) & $n$ \\
\midrule
Toy      & PPO & HIDDEN   & $0.039 \pm 0.073$ & 10 \\
Toy      & PPO & REVEALED & $0.726 \pm 0.100$ & 10 \\
Toy      & A2C & HIDDEN   & $0.195 \pm 0.352$ & 10 \\
Toy      & A2C & REVEALED & $0.842 \pm 0.025$ & 10 \\
510K     & A2C & SINGLE   & $0.644 \pm 0.201$ & 8 \\
510K     & A2C & STATIC   & $0.500 \pm 0.000$ & 8 \\
510K     & A2C & DYNAMIC  & $0.519 \pm 0.060$ & 8 \\
510K     & DQN & SINGLE   & $0.797 \pm 0.123$ & 8 \\
510K     & DQN & DYNAMIC  & $0.917 \pm 0.064$ & 8 \\
510K     & SAC & SINGLE   & $0.504 \pm 0.038$ & 2 \\
510K     & SAC & DYNAMIC  & $0.540 \pm 0.069$ & 2 \\
510K     & PPO & SINGLE   & $0.569$           & 1 \\
510K     & PPO & DYNAMIC  & $0.444 \pm 0.069$ & 9 \\
510K     & REINFORCE & SINGLE  & $0.487 \pm 0.312$ & 8 \\
510K     & REINFORCE & DYNAMIC & $0.605 \pm 0.238$ & 8 \\
Overcooked & PPO & STATIC  & $0.500 \pm 0.006$ & 8 \\
Overcooked & PPO & DYNAMIC & $0.000 \pm 0.000$ & 8 \\
Overcooked & A2C & STATIC  & $0.500 \pm 0.000$ & 8 \\
Overcooked & A2C & DYNAMIC & $0.125 \pm 0.217$ & 8 \\
Overcooked & DQN & STATIC  & $0.473 \pm 0.093$ & 8 \\
Overcooked & DQN & DYNAMIC & $0.645 \pm 0.209$ & 8 \\
\bottomrule
\end{tabular}
\caption{Complete $\kappa$ dataset across all environments, algorithms, and modes.}
\end{table}

\section{Continuous Reveal Data}

\begin{table}[h]
\centering
\small
\begin{tabular}{lcc}
\toprule
Reveal Fraction & $\kappa$ (mean $\pm$ std) & $n$ \\
\midrule
0\%   & $0.534 \pm 0.038$ & 2 \\
25\%  & $0.446 \pm 0.085$ & 2 \\
50\%  & $0.514 \pm 0.024$ & 2 \\
75\%  & $0.324 \pm 0.079$ & 2 \\
100\% & $0.616 \pm 0.039$ & 2 \\
\bottomrule
\end{tabular}
\caption{Continuous reveal $\kappa$ values at five information levels (510K, PPO).}
\end{table}

\section{Training Hyperparameters}

\begin{table}[h]
\centering
\small
\begin{tabular}{ll}
\toprule
Parameter & Value \\
\midrule
Learning rate     & $3 \times 10^{-4}$ \\
PPO n\_steps      & 2048 (self-play) / 256 (parallel) \\
Batch size        & 64 / 256 \\
Epochs per update & 10 \\
$\gamma$ (discount) & 0.99 \\
GAE $\lambda$     & 0.95 \\
Clip range        & 0.2 \\
Entropy coeff.    & 0.01 \\
Network           & MLP [256, 256], tanh activation \\
A2C n\_steps      & 256 \\
DQN buffer size   & 50,000 \\
DQN learning starts & 5,000 \\
DQN $\tau$ (target update) & 0.005 \\
DQN exploration   & $\epsilon$-greedy: $1.0 \to 0.02$ over 30\% of training \\
SAC $\alpha$      & Auto-tuned (initial 0.2) \\
REINFORCE lr      & $1 \times 10^{-3}$ \\
\bottomrule
\end{tabular}
\caption{Training hyperparameters for all algorithms.}
\end{table}

\section{Computing Infrastructure}

\begin{itemize}
\item GPU: NVIDIA GeForce RTX 5070 Ti Laptop GPU (12 GB VRAM)
\item CPU: Intel(R) UHD Graphics (128 MB, auxiliary)
\item RAM: 32 GB
\item OS: Windows 11
\item Framework: PyTorch 2.x, stable-baselines3 2.x, sb3-contrib 2.x
\item Environment: Gymnasium 0.29+, NumPy 1.24+
\end{itemize}

\section{Code Availability}

All source code is publicly available under the MIT License:

\begin{itemize}
\item \textbf{Main repository} (full experimental pipeline):
  \url{https://github.com/flavieninpekin/Information-Induced-Gradient-Contraction-in-Partially-Observable-MARL}
\item \textbf{Reproduction package} (curated for paper replication):
  \url{https://github.com/flavieninpekin/AAAI2027-510k-clear}
\item \textbf{510K environment} (standalone Gymnasium + PettingZoo package):
  \url{https://github.com/flavieninpekin/510k_env}
\end{itemize}

The reproduction package includes pre-computed results for validation,
config files for all experiment configurations, and a single-entry
\texttt{experiments/reproduce\_all.py} script to reproduce all paper results.

\end{document}
