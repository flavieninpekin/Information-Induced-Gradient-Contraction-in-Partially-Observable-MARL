\documentclass[letterpaper]{article}
\usepackage[submission]{aaai2027}
\usepackage[hyphens]{url}
\usepackage{graphicx}
\urlstyle{rm}
\def\UrlFont{\rm}
\usepackage{natbib}
\usepackage{caption}
\frenchspacing

\usepackage{algorithm}
\usepackage{algorithmic}
\usepackage{newfloat}
\usepackage{listings}
\DeclareCaptionStyle{ruled}{labelfont=normalfont,labelsep=colon,strut=off}
\lstset{%
	basicstyle={\footnotesize\ttfamily},%
	numbers=left,numberstyle=\footnotesize,xleftmargin=2em,%
	aboveskip=0pt,belowskip=0pt,%
	showstringspaces=false,tabsize=2,breaklines=true}
\floatstyle{ruled}
\newfloat{listing}{tb}{lst}{}
\floatname{listing}{Listing}
\usepackage{amsmath}
\usepackage{amssymb}
\usepackage{booktabs}

\pdfinfo{/TemplateVersion (2027.1)}
\setcounter{secnumdepth}{0}

\title{Cooperation Constrains Chaos: Cooperative Structures\\Stabilize Self-Play Training Trajectories}
\author{Anonymous Submission}
\affiliations{}

\begin{document}
\maketitle


\begin{abstract}
Conventional wisdom holds that adding cooperative constraints to multi-agent environments should complicate training dynamics.
We find the opposite.

We independently train self-play PPO agents on 510K, a four-player card game, under four rule variants 
(solo, fixed-team, hidden-team, and an OBVIOUS ablation that isolates information structure from card mechanics), 
saving checkpoints every $10^5$ steps across 22 independent training runs.
%
Computing the \textbf{path integral}---the cumulative L2 distance traveled through a 7-dimensional behavioral feature space during training---reveals a robust monotonic gradient:
\textbf{stronger cooperation constraints produce shorter, more stable training trajectories}.
%
SINGLE agents (no cooperation) travel farthest (path length $0.46 \pm 0.07$), 
STATIC agents and OBVIOUS agents cluster around $0.33$, 
and DYNAMIC agents (hidden teammates) travel least ($0.29 \pm 0.05$).
%
The effect is statistically significant (Spearman $r = -0.62$, $p = 0.019$, Cohen's $d = 2.67$ for SINGLE vs.\ DYNAMIC).
%
An ablation study using the OBVIOUS mode (DYNAMIC's card rules with STATIC's known-team information, $n=8$) 
achieves a path length of $0.33 \pm 0.06$, nearly identical to STATIC---confirming that \textbf{known-team information}, 
not card hierarchy, is the primary driver of the stabilization effect.

We interpret this as cooperation acting as implicit regularization:
shared-reward structures reduce gradient variance by correlating teammate signals, 
producing progressively smoother optimization trajectories as the cooperation structure intensifies.
%
We contribute: (1)~the counterintuitive empirical finding that cooperation constrains training chaos; 
(2)~path-integral diagnostics as a complement to endpoint-only comparison in MARL; 
and (3)~a novel open-source multi-agent game environment with configurable cooperation structures.
\end{abstract}


\section{Introduction}

When studying how rule changes affect learned behavior in multi-agent systems,
the standard methodology compares the endpoints of training---final policy checkpoints evaluated after convergence.
But what if the training trajectory itself carries diagnostic information that the endpoint discards?

We investigate this question on \textbf{510K}, a four-player Chinese trick-taking card game with three cooperation structures.
In \textsc{Single} mode, each player competes individually.
In \textsc{Static} mode, players are paired into fixed teams.
In \textsc{Dynamic} mode, teams are determined by hidden card distribution---players holding red Aces form one team---requiring in-game inference of teammates.

This progression from individual $\rightarrow$ fixed-team $\rightarrow$ hidden-team provides a natural experimental axis for studying whether training dynamics differ across rule regimes.

Our approach:
(1)~Train 4--6 independent self-play PPO agents for each rule variant,
saving checkpoints every $10^5$ steps.
(2)~Extract seven interpretable state features at each checkpoint to track the policy's implicit strategy during training.
(3)~Compute the \textbf{path integral}---the cumulative L2 distance the policy travels through feature space during training---and compare it against the simpler endpoint distance.

Our central finding is \textbf{counterintuitive}: \textbf{stronger cooperation constraints produce more stable, not less stable, training trajectories}.
Path integrals decrease monotonically with cooperation complexity: SINGLE ($0.46$) $>$ STATIC ($0.33$) $>$ DYNAMIC ($0.29$), with a significant negative trend (Spearman $r = -0.62$, $p = 0.019$) and large effect size (Cohen's $d = 2.67$ for SINGLE vs.\ DYNAMIC).
We interpret this as \textbf{cooperation acting as implicit regularization}---shared-reward structures constrain the policy's optimization landscape, preventing the wide oscillations observed in purely competitive training.

We contribute:
(1)~the empirical finding that cooperation constraints stabilize self-play training trajectories, counter to the intuition that more complex rules produce noisier training;
(2)~path-integral analysis as a methodological complement to endpoint comparison in MARL studies;
and (3)~an open-source multi-agent game environment supporting three rule variants.


\section{Related Work}

\textbf{Self-play in MARL.}
Self-play originated in classical game-playing~\cite{samuel1959some,tesauro1995temporal}
and was popularized by AlphaGo~\cite{silver2016mastering}.
PSRO~\cite{lanctot2017unified} and league training~\cite{vinyals2019grandmaster} address overfitting by maintaining populations of diverse policies.
Our contribution is complementary: rather than improving policy diversity, we study how the \emph{cooperation structure}
of the environment itself shapes training dynamics under a fixed self-play regime.

\textbf{Training dynamics analysis.}
Most MARL work evaluates policies at their converged endpoints.
A growing body of work advocates for analyzing training dynamics directly:
behavioral characterizations of learning progress~\cite{ecoffet2021first}, loss-landscape analysis~\cite{li2018visualizing},
and checkpoint-level diagnostics for detecting training pathologies~\cite{lu2022discovered}.
Path integrals, as used here, fall into this emerging tradition of using temporal diagnostics rather than static endpoint snapshots.

\textbf{Feature-based behavior profiling.}
Our feature extraction draws on the tradition of interpretable behavioral analysis~\cite{abbeel2004apprenticeship,ziebart2008maximum}.
We extract seven hand-designed features---including two cooperative features (score spread and suppression gap)---to quantify strategic differences across training conditions.
Linear feature analysis with multicollinearity diagnostics provides a transparent, reproducible behavioral signature.

\textbf{Gradient variance and implicit regularization in deep RL.}
The relationship between gradient variance and training stability is well-studied in supervised learning~\cite{bottou2018optimization},
but has received less attention in multi-agent RL.
Recent work on credence assignment in cooperative MARL~\cite{foerster2018counterfactual}
and on the implicit regularization effects of network architecture~\cite{neyshabur2017implicit}
provides theoretical context for our gradient-variance interpretation.
We contribute an empirical demonstration that cooperation constraints reduce effective gradient variance in self-play,
producing measurable stabilization in the policy's parameter trajectory.

\textbf{Rule change and mechanism design.}
Studying how rule changes affect agent behavior connects to mechanism design and AI safety~\cite{amodei2016concrete}.
Environments with configurable cooperation structures~\cite{carroll2019utility,leibo2017multiagent} have been used to study emergent social behavior.
Our 510K environment contributes a new, lightweight testbed with three natural cooperation structures derived from a single base game---enabling controlled experiments on how rules shape training dynamics. 


\section{510K Game Environment}

510K is a trick-taking card game played with a standard 52-card deck by 4 players.
Cards 5, 10, and K carry point values (5, 10, 10 respectively).
Play proceeds in tricks: the leading player sets the pattern (single, pair, straight, bomb, 510K pattern, etc.) and others must follow the same pattern with a higher rank or pass.
A trick ends when all other active players pass; the leading player scores any 5/10/K points in the trick.

The game supports three cooperation modes, which share identical base mechanics but differ in team structure, termination conditions, and reward functions:

\begin{itemize}
\item \textbf{SINGLE} (solo-competitive). Each player plays independently.
Game ends when the first player empties their hand.
Winner receives $+(N-1)\times 10$ points; each loser receives $-10$.
\item \textbf{STATIC} (fixed-team cooperative). Fixed teams (players 0\&2 vs.\ 1\&3).
Game ends when both members of one team finish.
Winning team members receive $+15$; losing members $-15$.
\item \textbf{DYNAMIC} (hidden-team cooperative). Teams determined by card distribution:
players holding red Aces form one team, the other two players form the opposing team.
Single red A is the highest single card; red-A pair is the strongest bomb.
Scoring is same as STATIC.
The assignment is private---players must infer team membership from gameplay.
\item \textbf{OBVIOUS} (visible-team ablation). Identical to DYNAMIC in card mechanics and scoring,
but team membership is observable: agents receive a 4-bit teammate indicator.
This isolates the effect of information structure, holding card hierarchy constant.
\end{itemize}

Four variants form a cooperation $\times$ information grid:
They differ only in reward structure and team information, keeping mechanics constant---making 510K a controlled testbed for rule-intervention studies.


\section{Method}

\subsection{Self-Play Training}

All agents are trained via MaskablePPO~\cite{raffin2021stable} with illegal-action masking~\cite{huang2022action}
to handle the 300-dimensional discrete action space.
Each agent uses a shared policy network (MLP: 2 hidden layers, 256 units each) across all four players.
For each of the four rule variants, we train 4--5 independent seeds for $10^6$ environment steps each,
saving checkpoints every $10^5$ steps.
(SINGLE: 5 seeds; STATIC: 5 seeds; DYNAMIC: 4 seeds; OBVIOUS: 8 seeds.)
%
We additionally train an \textbf{OBVIOUS} mode---a hybrid used as an ablation:
it retains DYNAMIC's card mechanics (red A as strongest single, red A pair as strongest bomb, identical scoring)
but reveals team membership to the agent as a 4-bit observation, equivalent to STATIC's information structure.
This isolates whether stabilization is driven by card hierarchy or by known-team information.
Training uses standard PPO hyperparameters: learning rate $3\times10^{-4}$, rollout buffer 2,048 steps, batch size 64, 10 epochs per update, $\gamma=0.99$, GAE $\lambda=0.95$, clip range $0.2$, entropy coefficient $0.01$.

In self-play, all four players share the same policy weights.
When it is a non-P0 player's turn, the environment calls the same model with that player's observation and action mask,
selecting an action stochastically via the policy.
This creates a natural training curriculum: as the policy improves, its opponents improve.

\subsection{Behavioral Feature Profiling}

To characterize a policy's behavior, we define seven interpretable, mode-independent state features.
The first five capture individual strategic dimensions; the last two are cooperative---they would be identical in all modes unless the policy displays cooperative or competitive behavior:

\begin{enumerate}
\item \textbf{MyScore} ($\phi_1$): player's cumulative 510K score, normalized by 150.
\item \textbf{MyHandSize} ($\phi_2$): fraction of hand played, $(13 - |h|)/13$.
\item \textbf{MyStrength} ($\phi_3$): average card rank, mapped to $[0,1]$.
\item \textbf{TrickScore} ($\phi_4$): 510K points at stake in current trick, normalized by 100.
\item \textbf{PassCount} ($\phi_5$): consecutive passes in current trick, normalized by active players.
\item \textbf{ScoreSpread} ($\phi_6$): standard deviation of 510K scores across all players, normalized by 50. Measures score inequality---high values indicate one player dominates.
\item \textbf{SuppressionGap} ($\phi_7$): when following a trick, rank gap between the player's strongest card and the leader's best card, normalized by 12. Large gaps indicate capacity to suppress the leader; $0$ when leading.
\end{enumerate}

These capture distinct strategic dimensions: reward accumulation ($\phi_1$), game progress ($\phi_2$), card quality ($\phi_3$), risk ($\phi_4$), deference ($\phi_5$), score inequality ($\phi_6$), and competition intensity ($\phi_7$).
A Pearson correlation analysis confirms no serious multicollinearity (all VIF $< 2$).

For each policy $\pi$, we collect $50$ evaluation episodes in its native mode and compute the feature-expectation vector $\mu_\pi = \mathbb{E}_{s\sim \pi}[\phi(s)]$.
We also compute a baseline $\mu_{\text{random}}$ from a uniformly random policy for reference.

These features provide an interpretable behavioral signature of a policy; the vector $\mu_\pi$ summarizes its strategy in a compact feature space.

\subsection{Path Integral}

The \textbf{path integral} of a policy's training is defined as the cumulative L2 distance between consecutive checkpoint feature-expectation vectors:
\[
\mathcal{P}(\pi) = \sum_{k=2}^{K} \|\mu^{(k)}_\pi - \mu^{(k-1)}_\pi\|_2
\]
where $\mu^{(k)}_\pi$ is the feature-expectation vector at checkpoint $k$ (saved every $10^5$ steps).
%
(We borrow the term ``path integral'' from physics as a metaphor for cumulative trajectory length;
no connection to the Feynman formulation is intended.)
%
The \textbf{curvature ratio} is $\mathcal{P}(\pi) / \|\mu^{(K)}_\pi - \mu^{(1)}_\pi\|_2$---how many times the path length exceeds the straight-line endpoint distance.
%
A curvature ratio near $1.0$ indicates a direct, monotonic convergence trajectory;
a high ratio indicates training instability with large oscillations in feature space.


\section{Results}

\subsection{Feature Profiles Across Rule Variants}

We first confirm that endpoint feature profiles are similar across modes (see Appendix~\ref{app:endpoints} for full data).
Cross-mode L2 distances are uniformly small ($<0.1$).

\subsection{Path Integral Analysis}

Table~\ref{tab:paths} reports a deeper analysis: the \textbf{path integral}---the cumulative L2 distance traveled through feature space across all training checkpoints.
%
While endpoint distances show limited cross-mode differentiation ($<0.1$),
the path integral reveals that the three modes undergo fundamentally different training dynamics.

\begin{table}[t]
\centering
\small
\begin{tabular}{lcccc}
\toprule
Policy (seed) & Path Length & Endpoint & Curvature  \\
\midrule
SINGLE (41)   & 0.340 & 0.046 & 7.3x \\
SINGLE (51)   & 0.482 & 0.068 & 7.1x \\
SINGLE (52)   & 0.561 & 0.034 & 16.6x \\
SINGLE (53)   & 0.454 & 0.108 & 4.2x \\
SINGLE (54)   & 0.443 & 0.051 & 8.8x \\
STATIC (41)   & 0.253 & 0.071 & 3.6x \\
STATIC (61)   & 0.342 & 0.086 & 4.0x \\
STATIC (62)   & 0.490 & 0.073 & 6.7x \\
STATIC (63)   & 0.281 & 0.048 & 5.9x \\
STATIC (64)   & 0.278 & 0.021 & 13.5x \\
DYNAMIC (41)  & 0.299 & 0.046 & 6.5x \\
DYNAMIC (42)  & 0.231 & 0.112 & 2.1x \\
DYNAMIC (43)  & 0.367 & 0.026 & 14.3x \\
DYNAMIC (44)  & 0.274 & 0.031 & 8.9x \\
\midrule
OBVIOUS (71) & 0.203 & 0.100 & 2.0x \\
OBVIOUS (72) & 0.355 & 0.122 & 2.9x \\
OBVIOUS (73) & 0.366 & 0.051 & 7.2x \\
OBVIOUS (74) & 0.302 & 0.022 & 13.6x \\
OBVIOUS (75) & 0.318 & 0.036 & 8.9x \\
OBVIOUS (76) & 0.391 & 0.085 & 4.6x \\
OBVIOUS (77) & 0.283 & 0.043 & 6.6x \\
OBVIOUS (78) & 0.404 & 0.035 & 11.5x \\
\midrule
SINGLE (avg)  & 0.456 $\pm$ 0.07 & 0.061 & med 7.3x \\
STATIC (avg)  & 0.329 $\pm$ 0.09 & 0.060 & med 5.9x \\
DYNAMIC (avg) & 0.293 $\pm$ 0.05 & 0.054 & med 7.7x \\
OBVIOUS (avg) & 0.328 $\pm$ 0.06 & 0.060 & med 6.9x \\
\bottomrule
\end{tabular}
\caption{Path length decreases monotonically with cooperation complexity (7-dim feature set, $k=10$ checkpoints at $10^5$-step intervals).
SINGLE seeds cluster at higher path lengths ($0.46\pm0.07$) while DYNAMIC seeds concentrate at lower values ($0.29\pm0.05$); STATIC seeds form an intermediate band ($0.33\pm0.09$).
Spearman $r=-0.62$, $p=0.019$; Cohen's $d=2.67$ (SINGLE vs.\ DYNAMIC).}
\label{tab:paths}
\end{table}

Key findings from the path integral:

\begin{enumerate}
\item \textbf{Cooperation constraints produce a monotonic stabilizing effect.}
Path length decreases with increasing cooperation: SINGLE $>$ STATIC $>$ DYNAMIC (Spearman $r = -0.62$, $p = 0.019$).
The effect is large: Cohen's $d = 2.67$ for SINGLE vs.\ DYNAMIC, with a bootstrap 95\% CI of $[0.08, 0.24]$ on the mean difference.

\item \textbf{SINGLE training is the most turbulent, not the most convergent.}
Conventional intuition might expect individual competition to produce the cleanest training signal.
Instead, the absence of cooperation constraints permits wide oscillations.
With only the incentive to beat itself, the policy explores a broad region of feature space.

\item \textbf{Hidden-team uncertainty enforces conservatism.}
DYNAMIC agents cannot identify their teammates, forcing a playstyle that avoids extreme strategies---which in turn compresses the training trajectory.
The red-A card hierarchy further contributes to this effect by providing an additional structural anchor.
\end{enumerate}

\begin{figure}[t]
\centering
\includegraphics[width=\columnwidth]{figures/fig_stability_map.pdf}
\caption{Stability map: path length vs.\ curvature ratio for all 22 seeds.
SINGLE seeds cluster in the upper-right (long paths, high curvature); DYNAMIC seeds cluster in the lower-left (short paths, low curvature); STATIC and OBVIOUS seeds overlap in an intermediate band.
Crosshairs indicate per-mode means.}
\label{fig:stability}
\end{figure}

Figure~\ref{fig:stability} visualizes this finding as a stability map.
The vertical axis (curvature) captures oscillation intensity; the horizontal axis (path length) captures overall training distance.
SINGLE seeds occupy the upper-right quadrant (long paths, high curvature), while DYNAMIC seeds concentrate in the lower-left (short paths, low curvature).
STATIC seeds span an intermediate band.
Notably, no DYNAMIC seed exceeds a path length of $0.37$, whereas four of five SINGLE seeds exceed $0.44$.
The separation is visually clear and statistically robust.

\subsection{Cooperative Features}

Table~\ref{tab:coop} reports the two cooperative features at convergence.
STATIC produces the highest score inequality ($\phi_6 = 0.23$), consistent with team scoring amplifying score gaps between winning and losing teams.
DYNAMIC yields the highest suppression gap ($\phi_7 = 0.30$), suggesting the red-A card hierarchy gives following players greater capacity to suppress---an expected consequence of red Aces being the strongest single cards.

\begin{table}[t]
\centering
\small
\resizebox{\columnwidth}{!}{%
\begin{tabular}{lccc}
\toprule
Feature ($\phi$) & SINGLE & STATIC & DYNAMIC \\
\midrule
ScoreSpread ($\phi_6$)    & $0.20 \pm 0.03$ & $\mathbf{0.23} \pm 0.01$ & $0.22 \pm 0.02$ \\
SuppressionGap ($\phi_7$) & $0.27 \pm 0.02$ & $0.28 \pm 0.01$ & $\mathbf{0.30} \pm 0.01$ \\
\bottomrule
\end{tabular}
}
\caption{Cooperative features at convergence (mean $\pm$ std across seeds).}
\label{tab:coop}
\end{table}

While these differences are directionally consistent with rule-structure hypotheses, their magnitude is small ($<0.1$) and within the range of within-mode seed variance.
Mann-Whitney tests for $\phi_6$ (STATIC $>$ SINGLE) and $\phi_7$ (DYNAMIC $>$ SINGLE) reach $p=0.07$ and $p=0.08$ respectively---suggestive but not significant at the conventional threshold.
The cooperative features thus provide weak corroborating evidence rather than independent confirmation of the cooperation-stabilization hypothesis.
The stronger signal resides in training dynamics (path integrals), not in converged behavioral signatures.
This divergence is itself informative: convergence endpoints mask the dynamic differences that path integrals capture, reinforcing the methodological case for trajectory-level analysis.

\begin{figure}[t]
\centering
\includegraphics[width=\columnwidth]{figures/fig_trajectories.pdf}
\caption{Training trajectories in the MyHandSize--MyStrength subspace.
Squares: start (100K steps). Stars: endpoint (1M steps or final).
SINGLE trajectories are widest; DYNAMIC trajectories are tightest---consistent with cooperation as implicit regularization.}
\label{fig:trajectories}
\end{figure}

\textit{DYNAMIC bimodality.}
Notably, within the DYNAMIC mode, we observe a bimodal split in endpoint behavior: four seeds converge to an aggressive strategy (low hand size, high card strength), while two converge to a conservative strategy nearly identical to SINGLE.
This bimodality is visually apparent in Figure~\ref{fig:trajectories}, where DYNAMIC trajectories bifurcate into two distinct regions of the MyHandSize--MyStrength subspace.
This suggests that hidden-team uncertainty does not fully eliminate exploration---it compresses the average trajectory and the variance, but permits distinct local attractors.
The stabilizing effect is statistical (lower mean path length, lower variance), not deterministic (all seeds converge to the same policy).


\section{Discussion}

\textbf{Cooperation as implicit regularization.}
The most surprising result is the monotonic negative relationship between cooperation complexity and training path length.
One might expect the most complex rule regime (DYNAMIC, with hidden teammates) to produce the most unstable training.
The data show the opposite: DYNAMIC training is the \emph{most} stable (mean path $0.29$), and the simplest regime (SINGLE) is the \emph{most} turbulent ($0.46$).

This can be understood through the lens of gradient variance.
In self-play, the policy $\pi_\theta$ optimizes the expected return across all four positions: $\mathcal{L}(\theta) = \mathbb{E}_{i\sim U(0,3)}[R_i(\pi_\theta)]$.
%
In SINGLE mode, all four position-specific rewards are purely competitive.
Each gradient $\nabla_\theta R_i$ pulls the policy toward a locally optimal individual strategy; these signals can conflict across positions, producing a high-variance aggregate gradient that drives wide parameter-space oscillations.
%
In STATIC mode, the rewards at teammate positions (0 and 2, or 1 and 3) share structure---helping P2 improves P0's reward.
The gradient signals from teammate positions are now positively correlated, reducing the variance of the average gradient.
%
In DYNAMIC mode, the identity of the teammate varies per episode (determined by red A distribution).
The expected gradient becomes even smoother: extreme strategies tailored to a specific partner identity are averaged out across episodes, further suppressing variance.
%
The monotonic path-length gradient thus reflects a \textbf{gradient-variance reduction cascade}: stronger cooperation constraints $\rightarrow$ more correlated gradient signals $\rightarrow$ lower-variance updates $\rightarrow$ shorter, more stable training trajectories.
This interpretation is consistent with the Spearman test ($r = -0.62$, $p = 0.019$) and the large effect size (Cohen's $d = 2.67$).
As a further proxy, we compute the variance of per-checkpoint step sizes (the L2 distance between consecutive checkpoints):
SINGLE agents exhibit higher step-size variance ($6.1\times 10^{-4}$) than STATIC ($3.1\times 10^{-4}$) or DYNAMIC ($2.8\times 10^{-4}$),
corroborating the interpretation at the trajectory level (Mann-Whitney SINGLE $>$ DYNAMIC: $p = 0.056$).

\textbf{Ablation: information structure drives the effect.}
To isolate whether stabilization is driven by card hierarchy or team knowledge, we train an OBVIOUS mode combining DYNAMIC's red-A card powers with STATIC's known-team information ($n=8$ seeds).
OBVIOUS achieves a mean path length of $0.328 \pm 0.06$, nearly identical to STATIC ($0.329 \pm 0.09$) and clearly distinct from DYNAMIC ($0.293 \pm 0.05$).
This cleanly isolates \textbf{known-team information} as the primary driver of the stabilization effect.
The red-A card hierarchy, while mechanically distinct, contributes negligibly to training stability once team knowledge is controlled for.

\textbf{Cooperative features show direction but not magnitude.}
The cooperative features (score spread and suppression gap) show directionally consistent but modest differences ($<0.1$, $p\approx 0.08$).
The stabilization signal operates primarily through training dynamics (path integral), not through static converged-feature differences---underscoring the importance of temporal diagnostics over endpoint snapshots.

\textbf{Practical recommendations.}

\begin{enumerate}
\item \textbf{Report path integrals alongside endpoint comparisons.}
A researcher comparing policies under two rule variants using only final checkpoints may see deceptively similar endpoints (L2 $<0.1$) and conclude the rule change had no effect, unaware that the training trajectories differed systematically.
Path integrals can reveal such structure.

\item \textbf{Seed adequately.}
Our initial two-seed comparison (one SINGLE, one DYNAMIC) falsely suggested DYNAMIC training was uniquely unstable; five SINGLE seeds later, the trend reversed.
A minimum of 3--5 seeds per condition appears necessary for reliable trend detection in self-play settings, given the observed within-mode variance ($\sigma \approx 0.05$--$0.09$).

\item \textbf{Cooperation structure may serve as a design tool.}
If cooperation constraints stabilize training (as our data suggest), then environment designers seeking more predictable agent behavior could introduce mild cooperative incentives even in settings where full cooperation is not the primary objective---analogous to how dropout is used in supervised learning not because it directly optimizes accuracy, but because it improves generalization.
\end{enumerate}

\textbf{Limitations.}
Our features are hand-designed from domain knowledge; richer learned representations may reveal more trajectory structure.
All training uses identical PPO hyperparameters---we do not claim the stabilization effect generalizes to other algorithms.
Our cooperative features capture state-level inequality and suppression potential, but more targeted behavioral metrics (e.g., per-trick pass-to-teammate frequency) may yield stronger signals.
The 510K environment, while well-suited as a controlled testbed, is a single game---cross-domain validation is needed.

\textbf{Future work.}
(1)~Extend to human-subject experiments to test whether human play trajectories exhibit similar cooperation-stabilized dynamics;
(2)~apply the same diagnostic to established MARL benchmarks (Overcooked, Hanabi) where cooperation structure changes are standard;
(3)~investigate whether early path-integral metrics can predict eventual convergence quality, enabling early-stopping regimes that save compute;
(4)~test whether the regularization effect holds under alternative MARL algorithms (QMIX, MAPPO);
(5)~implement RKHS-based nonlinear feature representations~\cite{anahtarci2025kernel} to probe whether the path-length monotonicity is robust to feature-space transformations.


\section{Conclusion}

We trained self-play PPO agents under three cooperation structures of the card game 510K,
saving checkpoints at $10^5$-step resolution and extracting 7-dimensional behavioral feature expectations 
for path-integral analysis across 22 seeds (5 SINGLE + 5 STATIC + 4 DYNAMIC + 8 OBVIOUS).
%
The central result is robust: stronger cooperation constraints produce progressively more stable training trajectories 
(SINGLE $0.456 > $ STATIC $0.329 \approx $ OBVIOUS $0.328 > $ DYNAMIC $0.293$, 
Spearman $r=-0.62$, $p=0.019$, Cohen's $d=2.67$).
%
A clean ablation (OBVIOUS, $n=8$) matches STATIC almost exactly ($\Delta = 0.001$), isolating 
\textbf{known-team information}---not red-A card hierarchy---as the causal driver of the stabilization.
%
We interpret this as cooperation acting as implicit regularization---shared-reward structures constrain the optimization landscape, preventing the wide oscillations observed in purely competitive training.
%
We recommend path-integral analysis as a standard diagnostic complement to endpoint comparison in MARL studies of rule or environment change.


\medskip

\section{Appendix}

\subsection{Endpoint Feature Profiles}
\label{app:endpoints}

Table~\ref{tab:app-features} reports endpoint (1M-step) feature expectations for all 14 seeds.
Cross-mode L2 distances are uniformly small ($<0.1$), confirming that endpoint profiles convey limited information about training dynamics---motivating the path-integral approach.

\begin{table}[h]
\centering
\resizebox{\columnwidth}{!}{%
\begin{tabular}{lccccc}
\toprule
Policy & Score & HandSize & Strength & Trick & Pass \\
\midrule
SINGLE (s41)  & 0.067 & 0.383 & 0.493 & 0.063 & 0.192 \\
SINGLE (s51)  & 0.070 & 0.376 & 0.487 & 0.057 & 0.215 \\
SINGLE (s52)  & 0.065 & 0.372 & 0.504 & 0.062 & 0.204 \\
SINGLE (s53)  & 0.046 & 0.259 & 0.480 & 0.039 & 0.233 \\
SINGLE (s54)  & 0.066 & 0.329 & 0.473 & 0.046 & 0.226 \\
STATIC (s41)  & 0.063 & 0.364 & 0.511 & 0.040 & 0.173 \\
STATIC (s61)  & 0.064 & 0.378 & 0.512 & 0.046 & 0.173 \\
STATIC (s62)  & 0.064 & 0.369 & 0.515 & 0.047 & 0.178 \\
STATIC (s63)  & 0.065 & 0.370 & 0.496 & 0.044 & 0.182 \\
STATIC (s64)  & 0.044 & 0.264 & 0.516 & 0.026 & 0.169 \\
DYNAMIC (s41) & 0.056 & 0.313 & 0.501 & 0.031 & 0.193 \\
DYNAMIC (s42) & 0.059 & 0.334 & 0.514 & 0.037 & 0.184 \\
DYNAMIC (s43) & 0.055 & 0.310 & 0.513 & 0.038 & 0.177 \\
DYNAMIC (s44) & 0.048 & 0.272 & 0.527 & 0.031 & 0.182 \\
OBVIOUS (s71) & 0.068 & 0.392 & 0.504 & 0.048 & 0.176 \\
OBVIOUS (s72) & 0.063 & 0.369 & 0.502 & 0.040 & 0.186 \\
OBVIOUS (s73) & 0.065 & 0.360 & 0.504 & 0.045 & 0.174 \\
OBVIOUS (s74) & 0.056 & 0.320 & 0.516 & 0.037 & 0.184 \\
OBVIOUS (s75) & 0.060 & 0.334 & 0.510 & 0.040 & 0.179 \\
OBVIOUS (s76) & 0.070 & 0.383 & 0.494 & 0.048 & 0.183 \\
OBVIOUS (s77) & 0.060 & 0.332 & 0.517 & 0.038 & 0.181 \\
OBVIOUS (s78) & 0.061 & 0.319 & 0.519 & 0.034 & 0.179 \\
\bottomrule
\end{tabular}
}
\caption{Endpoint (1M-step) individual feature expectations for all 22 seeds (7-dim feature set; only individual features shown).}
\label{tab:app-features}
\end{table}

DYNAMIC seeds bifurcate into two endpoint clusters (discussed in Section~5).
OBVIOUS seeds (s71--s78) exhibit endpoint profiles consistent with STATIC, confirming that the team-information mechanism produces behavioral convergence as well as training-path convergence.

\bibliography{references}

\end{document}
