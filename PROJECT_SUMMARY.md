# 510K + RL + IRL 项目文档

> 创建日期: 2026-06-27
> 状态: 环境就绪，三类策略已训练，两个诊断实验已完成

---

## 一、已有工作（已完成）

### 1.1 游戏环境
- 510K 完整规则实现：15种牌型（单、对、三、顺、连对、飞机、炸弹、510K同花/异花、红A单/对）
- 三种模式：SINGLE（单人）、STATIC（固定组队 0&2 vs 1&3）、DYNAMIC（红A组队，隐藏信息）
- Gymnasium 接口 + 非法动作掩码（300维动作空间，112维/116维观测空间）
- 完整的 CLI 人类对战 Demo

### 1.2 训练管线
- MaskablePPO + 动作掩码（sb3_contrib）
- 神经网络结构：pi=[256,256], vf=[256,256]
- 学习率3e-4，n_steps=2048，batch_size=64
- 已训练策略：
  - π_single：~2M步（final checkpoint 在 models/）
  - π_static：~1.8M步（final checkpoint 在 510k-env/models/）
  - π_dynamic：~2M步（final checkpoint 在 510k-env/models/）
- 所有策略均用 P0 vs 3 × random bot 的模式训练

### 1.3 平移评估管线
- `transfer.py`：将任一策略平移到三种规则下评估（4个玩家全部使用同一策略）
- 收集轨迹 + 特征提取 + 特征期望汇总
- 随机基线（500 episodes）

### 1.4 IRL管线
- `irl.py`：对比式IRL（w = μ_E - μ_R / λ）+ 表格法MaxEnt IRL
- `features.py`：5维决策相关特征

### 1.5 已完成的诊断实验

**实验A — 特征共线性诊断** 结果：通过。
```
所有VIF < 2（阈值5），只有MyScore↔MyHandSize在r=0.615
5维特征集在设计上是干净的。
```

**实验B — 线性IRL vs 非线性判别器** 结果：有分歧。
```
线性IRL认为 MyStrength 最重（权重0.79-0.97）
非线性NN判别器认为 MyHandSize 和 PassCount 最重
→ 线性模型可能遗漏了交互效应特征
```

### 1.6 三种策略的行为差异（核心数据）

```
                π_single    π_static    π_dynamic    Random
MyScore         0.070       0.055       0.068        0.095
MyHandSize      0.369       0.318       0.382        0.443  ← 关键差异
MyStrength      0.482       0.521       0.474        0.382  ← 关键差异
TrickScore      0.054       0.037       0.041        0.051
PassCount       0.196       0.178       0.189        0.186

L2距离：
  π_single vs π_static:  0.071
  π_single vs π_dynamic: 0.021
  π_static vs π_dynamic: 0.081
```

**反直觉发现**：π_dynamic（隐藏团队）的行为更接近 π_single（单人），而不是 π_static（固定团队）。隐藏信息没有催生合作行为。

---

## 二、可能的论文问题

### 问题A：策略的"规则流变"（原问题方向）

**核心问题**：当游戏规则从单人→固定团队→隐藏团队连续变化时，RL策略的隐式奖励函数如何流动？

**现状**：我们在三个不连续的点上观察到 π_dynamic "流回"了 π_single，但不知道中间的渐变过程。这不构成"流变"的完整叙事。

**需要的**：在规则参数的连续谱上训练更多策略（如：逐渐增加团队绑定强度、逐渐公开/隐藏队友信息），画出隐式奖励权重的"流变轨迹"。

**难度**：需要新增3-5个规则变体的训练（每个约2h计算），和一个参数化的规则空间。

**发表潜力**：
- 如果结果连续且可解释 → CoG/AAMAS 有力
- 如果发现非单调、非线性流变模式 → AAAI/ICLR 的spotlight可能

### 问题B：线性IRL假设的充分性检验（方法论方向）

**核心问题**：在多智能体棋牌游戏中，线性可解释特征的IRL和非线性（RKHS/神经网络）IRL在什么情况下给出不同结论？差异有什么结构？

**已有的**：判别器实验已发现 PassCount 特征被线性模型遗漏。这初步表明线性假设有局限。

**需要的**：
- 实现真正的核IRL或神经网络MaxEnt IRL（不只是判别器）
- 在三种策略上系统对比线性 vs 非线性的奖励恢复质量
- 分析差异的来源：交互效应？状态依赖的价值反转？

**难度**：实现核IRL约1-2周，实验约1周。

**发表潜力**：
- 这是"用510K验证一个通用方法论问题"的路线
- 如果对比设计严谨 → AAAI有可能（方法贡献）
- 即使没有核IRL的收敛证明，清晰的实证对比也有价值 → AAMAS/CoG

### 问题C：IRL作为策略行为的诊断工具（应用方向）

**核心问题**：给定一个策略在两个规则下的表现差异，IRL能否诊断出差异的来源是环境动力学还是策略适应？

**已有的**：平移实验验证了同一策略在不同规则下IRL恢复相似权重。

**问题**：用户已指出这个二分法本质上trivial——隐式奖励一定变，显式奖励不变，没有信息量。

**出路**：把问题改成"IRL能否量化行为差异中哪些部分可归因于特征A，哪些可归因于特征B"。奖励分解（reward decomposition）是一个有实质内容的方向。

**难度**：需要实现奖励分解方法（已有初步的对比式IRL可以做简单的分解）。

**发表潜力**：如果分解方法有通用性 → CoG/AAMAS。但不够AAAI。

### 问题D：规则→奖励映射的LLM可预测性（交叉方向）

**核心问题**：用自然语言描述游戏规则，LLM能否预测在该规则下训练的RL智能体的隐式奖励函数？LLM的"机制设计直觉"和实际结果有多大差距？

**已有的**：三种规则描述 + 三种IRL权重。

**需要的**：
- 更多规则变体（至少5-6种）才能让LLM预测有意义
- 设计规则描述模板
- 对比LLM预测 vs 实际IRL结果
- 分析LLM在哪类规则上预测准、哪类不准

**难度**：需要额外3-5个规则变体的训练（每个~2h）。LLM API调用工作不大。

**发表潜力**：
- 跨领域趣味性高（游戏 × LLM × IRL）
- 如果发现了LLM系统性的预测偏差 → AAAI/NeurIPS的交叉领域有空间
- 如果只是"LLM预测了正确的方向" → 偏弱，适合CoG/workshop

### 问题E：团队信息结构对合作行为的影响（实证方向）

**核心问题**：在固定团队（完全信息）和隐藏团队（不完全信息）两种合作结构下，RL智能体的行为差异是否显著？

**已有的**：π_static 和 π_dynamic 的L2=0.081——有差异，但并不大。π_static（固定团队）有最强的留牌策略和最慢的出牌速度。

**需要的**：
- 更深入的行为分析（不只是特征期望，还有回合级行为模式）
- 更多的随机种子训练来验证差异的统计显著性
- 可能需要人类玩家的基线对比

**难度**：小。在现有数据上多做分析即可。

**发表潜力**：标准的实证报告，适合CoG。但缺乏方法论贡献，不达AAAI。

---

## 三、业界关注的问题

### 3.1 游戏公司的AI测试（腾讯/网易/米哈游）

**需求**：游戏策划频繁调整规则（得分倍率、组队方式、技能平衡），每次调整后需要重新测试AI行为是否按预期变化。目前的流程是训练一个新策略（2h+），然后人工观察行为。

**我们的工具可以提供的**：
- IRL诊断：调整规则后，策略的隐式奖励权重如何变化？变化方向和策划预期一致吗？
- 潜在价值：在训练前预测策略变化方向，减少试错成本。

**需要回答的问题**："策划的一个规则改动 → 智能体的'价值观'偏移了X、Y方向——这个偏移是可预测的吗？"

### 3.2 AI Safety / 奖励迁移

**需求**：在RLHF中，模型的奖励函数在训练环境和部署环境之间是否发生非预期的偏移（reward misspecification / reward hacking）。

**我们的工具可提供的**：
- IRL作为诊断工具：检测策略在不同环境下是否保持了相同的隐式奖励结构
- 如果奖励结构不变 → 环境变化是安全的
- 如果奖励结构偏移 → 需要重新训练或约束

**需要回答的问题**："一个策略从训练环境迁移到规则微变的环境后，它的隐式奖励函数是否稳定？"

### 3.3 可解释的奖励建模

**需求**：用少量可解释特征去化约复杂行为的奖励结构，让人能理解"AI在追求什么"。

**我们的工具可提供的**：
- 5维可解释特征的IRL恢复（MyScore, MyHandSize, MyStrength, TrickScore, PassCount）
- 线性vs非线性对比揭示"哪些行为维度是线性可控的，哪些不是"

**需要回答的问题**："5维特征够不够描述一个复杂博弈策略的奖励结构？不够的部分长什么样？"

---

## 四、当前位置总结

```
已完成的：环境、训练、IRL管线、特征诊断、线性vs非线性初步对比
未完成的：论文要回答什么问题（核心，未定）
时间线：AAAI-27 7月28日截稿（约4周）
资源：单GPU，你是独立研究者
```

**三个可选策略**：

| 策略 | 投入 | 中稿概率 | 会议 |
|------|------|---------|------|
| 用现有数据写实证报告 | 2周写作 | 高（>80%） | CoG / AAMAS |
| 实现核IRL做方法对比 | 3周（2周实现+1周写作） | 中（40-50%） | AAAI / AAMAS |
| 加规则变体做LLM预测 | 3周（1周训练+1周LLM+1周写作） | 中（50%） | AAAI / AAMAS |

---

## 五、下一步

需要你决定的是：
1. **要回答的问题**（A、B、C、D、E 或其他）
2. **目标会议**（AAAI 7.28 / AAMAS 10月 / CoG 2027春）
3. **是否做额外实验**（核IRL / 更多规则变体 / LLM预测，还是用现有数据）

定下来之后，可以在这个文档基础上扩展为论文框架+写作计划。
